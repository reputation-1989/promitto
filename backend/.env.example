# MongoDB Connection
# For local development, use a free MongoDB Atlas cluster: https://www.mongodb.com/cloud/atlas
# Or install MongoDB locally: https://www.mongodb.com/try/download/community
MONGODB_URI=mongodb://localhost:27017/promitto
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/promitto

# JWT Secret (Generate a random string)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3001

# Twilio Configuration (OPTIONAL - only needed for production SMS)
# Get credentials from: https://www.twilio.com/console
# Leave empty for development mode (OTP will be 123456)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Redis (OPTIONAL - only needed for production)
# For development, OTP is stored in memory
REDIS_URL=